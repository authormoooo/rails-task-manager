<h3><%= @task.title %></h3>
<p><%= @task.completed %></p>
<p><%= @task.details %></p>
<%= link_to "Back to the list",  tasks_path%>
<%= link_to'Edit the task', edit_task_path %>
<%= link_to "Delete",
    task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "U sure?"}
    %>
